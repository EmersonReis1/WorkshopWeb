@model WorkshopWeb.Data.Entities.Reparation
@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<from method="post">

    <div class="form-group ">

        @for (var i = 0; i < Model.ServiceDetails.Count(); i++)
        {
            @*<div class="custom-control custom-checkbox">
                    <input asp-for="@Model.ServiceDetails[i].State.Selected" type="checkbox" class="custom-control-input" />
                    <label class="custom-control-label" asp-for="@Model.Services[i].Selected">@Model.Services[i].Text</label>
                    <input type="hidden" asp-for="@Model.Services[i].Value" />
                    <input type="hidden" asp-for="@Model.Services[i].Text" />
                </div>*@

            <div class="form-group">
                <div class="custom-control custom-switch">
                    <input type="checkbox" class="custom-control-input" asp-for="@Model.ServiceDetails[i].State">
                    <label class="custom-control-label" for="customSwitch1">@Model.ServiceDetails[i].Service.Name</label>
                </div>
            </div>
        }



    </div>
    <div class="form-group">

        @*<a asp-action="Save"  class="btn btn-success" >Save</a>
        <a asp-action="Finish"  class="btn btn-success">Finish</a>*@
    </div>
</from>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}